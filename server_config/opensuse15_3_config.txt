//ENABLE SSH
sudo rcsshd start
sudo /sbin/yast
    > Security and Users
        > Firewall
            > external
                > ssh

//CONFIGURE DNS
sudo zypper install bind
sudo vi /etc/named.conf

    options
        > listen-on port 53 { 127.0.0.1; 192.168.88.150; }
        > forwarders {1.1.1.1; 8.8.8.8;}
        > allow-query { any;};

    zone   "miracleproject.online"        {
        type master;
        file    "/var/named/ns.miracleproject.online";
        allow-update {none;}
    };

    

sudo vi /etc/named/ns.miracleproject.online.zone
    $TTL 1W
    @               IN SOA ns.miracleproject.online root.miracleproject.o186.103.186.18:6922nline (
                                    2               ; serial (d. adams)
                                    2D              ; refresh
                                    4H              ; retry
                                    6W              ; expiry
                                    1W )            ; minimum

    $ORIGIN miracleproject.online.
    miracleproject.online. IN NS ns.miracleproject.online.
    ns IN A 127.0.0.1

//INSTALL nginx
sudo zypper update
sudo zypper install nginx
sudo systemctl enable nginx.service
sudo systemctl start nginx.service
sudo firewall-cmd --get-default-zone
sudo firewall-cmd --zone=public --add-service=http --permanent
sudo firewall-cmd --zone=public --add-service=https --permanent
sudo firewall-cmd --reload

